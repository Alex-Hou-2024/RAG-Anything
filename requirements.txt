# Base runtime dependencies. These are intentionally lightweight enough for
# the API service to start without MinerU, Docling, or PaddleOCR.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1
boto3>=1.35,<2
SQLAlchemy>=2.0,<3
psycopg[binary]>=3.2,<4
pydantic>=2,<3
Pillow>=10.0.0
pypdf>=5,<6
python-dotenv>=1.0,<2
huggingface_hub
lightrag-hku<1.5
tqdm

# Optional heavyweight parsers are declared as package extras rather than
# installed by default:
#   pip install 'raganything[mineru]'
#   pip install 'raganything[docling]'
#   pip install 'raganything[paddleocr]'
