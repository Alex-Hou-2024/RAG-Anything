import {citations} from '../../components/Citations/index.js';
export function mountChat(root){root.innerHTML='<main><h1>对话问答</h1><div id="messages"></div><form><select><option>hybrid</option><option>local</option><option>global</option><option>naive</option><option>mix</option></select><input aria-label="问题" required><input type="file" accept="image/*"><button>发送</button></form></main>';const form=root.querySelector('form'),list=root.querySelector('#messages');form.onsubmit=async e=>{e.preventDefault();const [mode,input,file]=form.elements,q=input.value;const row=document.createElement('article');row.textContent='…';list.append(row);let body={query:q,mode:mode.value,stream:true},url='/query';if(file.files[0]){body.multimodal_content=[{type:'image',image_data:await fileToBase64(file.files[0])}];url='/query/multimodal'}const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const text=await r.text();let answer='',refs=[];text.split('\n\n').forEach(x=>{if(x.includes('event: delta')) answer+=JSON.parse(x.split('data: ')[1]).text;if(x.includes('event: citations')) refs=JSON.parse(x.split('data: ')[1]).citations});row.textContent=answer;row.append(citations(refs));form.reset()};}
function fileToBase64(f){return new Promise((ok,no)=>{const r=new FileReader;r.onload=()=>ok(r.result.split(',')[1]);r.onerror=no;r.readAsDataURL(f)})}
