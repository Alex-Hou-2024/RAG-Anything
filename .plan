# 配置引导架构计划（Issue #65–#70）

## 模块与共享契约
- `api/routers/configuration.py`：提供配置清单读取与模板生成的同源 `/api/configuration/*` 接口；只暴露变量名、状态、说明和示例，绝不返回实际密钥。
- `api/services/configuration.py`：集中定义 `ConfigurationItem`、类别、是否必填、就绪判断、依赖关系、模板渲染与安全脱敏规则；复用 `Settings`、能力探测和 RAG 健康状态，避免前后端各自推断配置。
- `api/routers/health.py`：以可消费的未就绪原因/操作建议补充既有健康契约，供文档、问答和引导页统一使用。
- `web/src/pages/configuration.js`：新增“配置指南”页面，使用同一清单 API 显示必填配置、运行目录、模型组、可选解析依赖和操作步骤。
- `web/src/api/configuration.js`、`web/src/router.js`、`web/src/components/layout.js`：增加 API 客户端、路由 `/configuration` 与导航入口；所有文案为中文。
- `tests/api/`：覆盖清单不泄密、模板生成、缺失/无效配置状态和健康契约；`tests/e2e/` 覆盖从未就绪提示跳转至配置指南并恢复可用的路径。

## 依赖顺序
1. #65 建立配置指南页面、导航和路由骨架。
2. #66 定义服务端配置清单契约，供页面和后续功能共享。
3. #67 基于清单生成安全、可复制的 `.env` 模板（只含占位值）。
4. #68 让文档/问答未就绪状态链接到配置指南，并显示具体下一步；依赖 #65/#66。
5. #69 将能力探测结果映射为可选依赖安装说明；依赖 #66，展示到 #65 页面。
6. #70 使用真实路由和受控测试双端验证完整引导；依赖全部前述实现。

## 关键设计决定
- 配置清单是唯一事实来源：前端只渲染 API 契约，绝不内嵌密钥、模型密钥状态细节或重复的配置规则。
- 所有密钥仅以“已配置/未配置”状态暴露；模板使用明确占位符，日志和响应不回显值。
- 模型配置按既有成套校验分组（LLM、视觉、Embedding）；可选解析器与核心可用性分离，缺少可选依赖仅提示影响和安装方式。
- 引导在同源 API 和现有代理感知 CORS 规则下工作，不新增认证、外部服务或独立前端进程。
